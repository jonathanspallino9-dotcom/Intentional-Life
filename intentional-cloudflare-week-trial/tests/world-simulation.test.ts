import {describe,expect,it} from 'vitest';
import {ART_TILE_PX,SETTLEMENT_GRID,canPlaceWorld,depthValue,entranceClearance,sortByDepth,type WorldObject} from '../src/game/world';
const obj=(id:string,x:number,y:number,width:number,height:number,placementClass:WorldObject['placementClass']='TALL'):WorldObject=>({id,x,y,width,height,placementClass});
describe('locked world geometry',()=>{it('uses 16px native tiles on a 24x20 logical settlement',()=>{expect(ART_TILE_PX).toBe(16);expect(SETTLEMENT_GRID).toEqual({width:24,height:20})})});
describe('Y depth simulation',()=>{it('puts lower/southern roots in front even when northern sprite could be visually taller',()=>{const hugeNorth=obj('huge-north-tree',5,5,2,2);const smallSouth=obj('small-south-tree',5,8,1,1);expect(sortByDepth([smallSouth,hugeNorth]).map(x=>x.id)).toEqual(['huge-north-tree','small-south-tree'])});it('sorts tree behind house when its root is north, and in front when south',()=>{const house=obj('house',8,8,5,4,'STRUCTURE');const northTree=obj('north-tree',9,4,2,2);const southTree=obj('south-tree',9,13,2,2);expect(sortByDepth([house,northTree]).map(x=>x.id)).toEqual(['north-tree','house']);expect(sortByDepth([house,southTree]).map(x=>x.id)).toEqual(['house','south-tree']);expect(depthValue(southTree)).toBeGreaterThan(depthValue(house))})});
describe('clearance simulation',()=>{const house=obj('house',8,8,5,4,'STRUCTURE');const zone=entranceClearance({id:'house',type:'COTTAGE',name:'Cottage',level:1,x:8,y:8,width:5,height:4,built:true,overgrown:false});it('blocks tall objects from the two-cell entrance lane',()=>{expect(canPlaceWorld(obj('tree',9,12,2,2,'TALL'),[house],[zone])).toBe(false)});it('allows small planting in the entrance lane',()=>{expect(canPlaceWorld(obj('flower',10,12,1,1,'SMALL'),[house],[zone])).toBe(true)});it('allows a tall tree beside the facade if footprints do not collide',()=>{expect(canPlaceWorld(obj('tree',13,9,2,2,'TALL'),[house],[zone])).toBe(true)})});
